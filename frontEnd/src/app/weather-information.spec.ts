import { WeatherInformation } from './weather-information';

describe('WeatherInformation', () => {
  it('should create an instance', () => {
    expect(new WeatherInformation()).toBeTruthy();
  });
});
